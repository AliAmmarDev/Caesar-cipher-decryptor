void PrintHex(int fileSize, unsigned char* charBuffer); //routine to print data in hexadecimal form

void DecryptionWithKey(int fileSize, unsigned char* charBuffer); //routine to print decrypted characters

int KeylessDecryption(int fileSize, unsigned char* charBuffer); //routine to find the key for keyless files/messages




